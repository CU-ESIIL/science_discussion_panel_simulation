# Configuration

This directory holds human-readable working-group configuration.

Use `working_group.yaml` for project identity, branding, lifecycle state, enabled integrations, model-routing notes, storage posture, and workflow modes.

Do not put secrets here. Use `.env`, GitHub Secrets, Docker secrets, Kubernetes Secrets, or deployment-specific credential stores.
