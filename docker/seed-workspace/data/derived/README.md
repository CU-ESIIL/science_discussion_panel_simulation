# Derived Data

Place model outputs, summaries, diagnostics, and generated tables here when they can be reproduced from scripts.

Each derived file should link to the generating script, inputs, parameters, and review status.
