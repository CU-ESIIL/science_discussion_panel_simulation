# Datasets

Register datasets here when the project needs a durable, human-readable inventory separate from raw files.

For each dataset, document source, access method, format, license, citation requirements, sensitivity, and whether it is stored locally, streamed remotely, cached, or mounted from external storage.
