apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scienceclaw-data
  namespace: scienceclaw
  labels:
    app.kubernetes.io/name: scienceclaw
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
