name: Scientific discussion panel smoke test

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run panel smoke test
        run: scripts/test-panel.sh

      - name: Run ScienceClaw layout smoke test
        run: scripts/test-scienceclaw-layout.sh

      - name: Run spatiotemporal runtime smoke test
        run: scripts/test-spatiotemporal-runtime.sh

      - name: Run secret smoke test
        run: scripts/test-secrets.sh
