# GitHub Repositories

This repository snapshot is currently inside the ScienceClaw container repository. Future public release work should use a dedicated project repository after human approval.

| Repository | Purpose | Status |
| --- | --- | --- |
| `openclaw_container` / `projects/fractal_corridors` | preserved internal project snapshot | current |
| `TBD/fractal-corridors-public` | future public release repo | not created |

Gateway 3 manages live authorized repositories through:

```text
/workspace/.openclaw-github/authorized-repos.yaml
```

Do not store GitHub credentials here.
