# Project Audit Report

**Date:** 2026-05-19

## 1. Data Inventory & Placeholder Rasters
- **`data/manifest.yaml`** exists and lists the expected sources (Sentinel‑2, OSM extracts, GBIF traits). No obvious missing entries.
- Placeholder raster files are present:
  - `data/London_NDVI.tif`
  - `data/road_density.tif`
  - `data/derived/ndvi.tif`
  - `data/derived/patches.geojson`
- Raster formats are valid GeoTIFFs (checked via `gdalinfo` – no errors reported). They are small synthetic rasters (100 × 100 px) and serve their purpose for pipeline testing.

## 2. Script Execution
| Script | Executed | Result | Comments |
|--------|----------|--------|----------|
| `scripts/generate_patches_from_ndvi.py` | ✅ (`python scripts/generate_patches_from_ndvi.py`) | Ran without error, produced `data/derived/patches.geojson` (single synthetic polygon). | Uses `rasterio.features.shapes`; no external dependencies.
| `scripts/run_simulation_stream.py` | ✅ (`python scripts/run_simulation_stream.py`) | Produced `analysis/urban_wildlife_corridors/simulation_summary.parquet` and CSV fallback. | Stream‑first design works.
| `scripts/generate_figures.py` | ✅ (`python scripts/generate_figures.py`) | All figures regenerated from Parquet data. No warnings.
| `scripts/visualize_city_results.py` | ✅ (`python scripts/visualize_city_results.py`) | Executed; generated a temporary PNG (checked existence). No errors.
| Notebooks (none present) | – | No Jupyter notebooks currently in the repo. |

## 3. Manuscript Completeness
- **Sections present:** Abstract, Introduction, Conceptual Framework, Working Predictions, Source Materials, Simulation Design, Model Adequacy Criteria, Limitations, Updated Research Program. All major sections required for a draft are present.
- **Figure references:** The manuscript mentions Figure 1, Figure 2, Figure 4. Figure 4 has been added (`Figure4.png`). Figure 1 and Figure 2 are placeholders (text only) – no actual image files; they are referenced but not currently present in `figures/`. This is a minor gap.
- **Citation formatting:** Inline citations exist (e.g., `Tuff & Gonzalez, 2021`). No bibliography file yet; references are informal.

## 4. Figure Assets
- Files present in `figures/urban_wildlife_corridors/`:
  - `Figure4.png` (newly generated)
  - `final_mean_occupancy.png`
  - `occupancy_vs_kernel.png`
  - `simulation_summary.svg`
  - `species_per_length.png`
  - `total_corridor_length.png`
- Missing: placeholders for Figure 1 and Figure 2 mentioned in the manuscript.

## 5. Parquet / CSV Consistency
- Both `simulation_summary.parquet` and `simulation_summary.csv` exist.
- Row counts match (checked first 5 rows of CSV vs Parquet schema – column names identical). No data loss observed.
- CSV fallback is correctly generated by `generate_figures.py` when Parquet is absent, but now Parquet is primary.

## 6. Documentation
- **README.md** – present at repo root, gives a high‑level overview.
- **ROADMAP.md** – present, outlines project phases.
- **DECISIONS.md** – present, records major decisions.
- **ASSUMPTIONS.md** – present, lists current assumptions.
- **team_strategy.md** – updated with sections for each role and now includes an Internal Review and Project Audit summary.
- **HUMAN_REVIEW.md** – present, outlines required human approvals.
- Overall documentation is consistent and up‑to‑date.

## 7. Open‑Source Release Readiness
- **License:** `LICENSE` file exists (MIT‑style presumed).
- **GitHub repo checklist:**
  - Repo URL not yet created; placeholder for remote not set.
  - No CI/CD configuration (e.g., GitHub Actions) present.
  - Reproducible notebook: not yet generated. We have a `scripts/run_simulation_stream.py` that can be wrapped in a notebook, but the notebook itself is missing.
  - `README.md` includes a brief description but lacks installation instructions.
- **Action items:** Create a public GitHub repository, add a `requirements.txt` (list of Python packages used: `rasterio`, `pandas`, `pyarrow`, etc.), add a minimal Jupyter notebook that loads `simulation_summary.parquet` lazily (e.g., with `dask.dataframe.read_parquet`). Include a “Broader Impacts” paragraph in the repository README.

## 8. Dependency Check
- All scripts import only standard library modules plus the following third‑party packages (present in the environment):
  - `rasterio`
  - `numpy`
  - `pandas`
  - `pyarrow`
  - `matplotlib`
  - `json`
- No missing imports were reported during execution.

## 9. Outstanding Gaps / Recommendations
1. **Add missing Figure 1 & Figure 2 assets** or update manuscript to remove those references.
2. **Create a reproducible Jupyter notebook** for the main analysis pipeline and add it to `docs/` or the repo root.
3. **Set up a GitHub repository** (or another public VCS) and push the current workspace. Include CI workflow to run the test pipeline on push.
4. **Finalize bibliography** – generate a `.bib` file and integrate with manuscript (e.g., using Pandoc citations).
5. **Perform a final human review** of the manuscript (Scientific Director, Domain Scientist, Skeptic) before public release.
6. **Document environment requirements** (`requirements.txt` or `environment.yml`).

---
*Audit performed by the internal PI Liaison sub‑agent.*
