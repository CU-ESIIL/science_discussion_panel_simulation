# 2026-05-18 Memory

## Urban Wildlife Corridors Project

- User started a new environmental data science project on urban wildlife corridors. Core hypothesis: corridor design should move beyond shortest-distance links among habitat fragments toward hierarchical, dendritic/fractal transport-network-like geometries that may better support dispersal, coexistence, and recovery.
- Initial manuscript and toy simulation scaffold were created under `documents/urban_wildlife_corridors/`, `scripts/`, `analysis/urban_wildlife_corridors/`, and `figures/urban_wildlife_corridors/`. Early deterministic results were recognized as too stabilizing and not adequate for equilibrium or coexistence claims.
- User correctly challenged that the original run was not long enough and that equilibrium may not be achievable or the right target. Project direction shifted toward non-equilibrium metrics: extinction timing, recolonization, rescue effects, quasi-stationarity, disturbance recovery, stochasticity, and cost-matched topology controls.
- User provided prior work in `/workspace/documents/source_materials`: `Design principles for fitting cities and ecosystems into the same footprint 14 April 2021.docx`, `population dynamics on a network.qmd`, and `Greenspace scaling.Rmd`.
- Prior work synthesis changed the framing: this is not simply dendritic corridors versus shortest paths on generic graphs. It should be framed as co-located city/ecosystem transport networks, comparing ecological corridor geometries by flow, effective travel time, crossing risk, disturbance recovery, extinction/recolonization, and support for species with different movement scales.
- `population dynamics on a network.qmd` is the preferred mathematical base for the next manuscript-scale model: node-specific `r`/`K`, graph-distance dispersal, stochasticity, extinction timing, and multi-species competition. The JS simulation should remain a diagnostic scaffold unless rebuilt around this equation family.
- `Design principles...` contributes design motifs and theory language: trunks, branches, rings, co-located urban/ecological infrastructure, effective time/risk costs, and human-network crossing penalties.
- Source access blocker was resolved when user added files locally. Earlier Slack file download failed with `missing_scope` and public Slack links returned 404; this is now obsolete for these files.
- User requested: “consult the whole team and update everyone on this. try to come up with an updated and refined manuscript.” Work had begun by reading project files and should continue by updating team artifacts, role reports, and the refined manuscript draft. Durable output targets likely include `TEAM_BRIEF.md`, `PROJECT_CHARTER.md`, `INITIAL_TASKS.md`, `QUESTIONS_FOR_USER.md`, `ASSUMPTIONS.md`, role reports under `agent_reports/`, and `documents/urban_wildlife_corridors/manuscript_draft.md`.

## Project Standards To Preserve

- Treat scientific claims as provisional until tied to evidence and reviewed. Skeptic review is required before promoting major claims.
- Do not treat dendritic corridors as confirmed better. Current defensible hypothesis: hierarchical/ecological transport networks may improve robustness, recovery, or efficiency under realistic stochastic disturbance and movement-cost constraints.
- Human review is required before external/public claims, publication-ready recommendations, sensitive community/policy/health/legal claims, file deletion, external API use with implications, installed skills, credential changes, or broad mounts.
## 2026-05-18 22:33 UTC - Sub-agent updates

- **Data Engineer sub-agent `c4116780-...`** reported completion of the `generate_patches_from_ndvi` task. The script `scripts/generate_patches_from_ndvi.py` has been written and a `generate_patches` target added to the Makefile.
- However, the required NDVI raster (`data/derived/ndvi.tif`) is not present, so running the patch generation script would fail (no input data). The previous `download_ndvi_sentinel2.py` script was updated to support streaming via GDAL/VSICURL (`--stream` flag) per the sub‑agent’s reminder to prefer streaming over full downloads.
- **Action needed:** Run the NDVI download with streaming, e.g., `make download_ndvi` (or directly `python scripts/download_ndvi_sentinel2.py --tile T31TFJ --date 2023-06-15 --output data/derived/ndvi.tif --stream`). Once the NDVI file exists, invoke `make generate_patches` to create `data/derived/patches.geojson`.

- **Data Engineer sub-agent `c8894df8-...`** completed the GBIF species‑traits fetch. The script `scripts/fetch_gbif_species_traits.js` has been added and a `fetch_gbif_traits` Makefile target is available. The resulting file `data/derived/species_traits_gbif.json` contains 100 species entries, but all `meanDispersalMeters` fields are null (the heuristic could not locate dispersal distances). This output is stored and may need manual curation or a more sophisticated trait extraction approach.

- **Next steps for the workflow:**
  1. Execute the NDVI streaming download.
  2. Generate habitat patches.
  3. Re‑run the full pipeline (`make pipeline`) to incorporate the new landscape and species traits.
  4. Update the manuscript draft accordingly.

No further automatic actions are taken pending user confirmation or execution of the above commands.
